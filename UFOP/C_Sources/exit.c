
#include "exit.h"
//#include "timer.h"
//#include "music.h"
//#include "sound.h"

void exit_all(int selector)
{
/*
    selector = 3;
    
    int v1;
    int Buffer;
    if (selector == 1)
    {

        v1 = _fopen("XCOM3.CFG", "rb+");
        if (!v1)
        {
            printf("Cannot open file 'XCOM3.CFG'.\n");
            _exit(1);
        }
        if (_fwrite(&Buffer, 1u, 1u, v1) != 1)
        {
            printf("Cannot fwrite file 'XCOM3.CFG'.\n");
            _exit(1);
        }
        _fclose(v1);
        __execl("XCOMAPOC.exe", "XCOMAPOC.exe",5, 5, 0);
    }
    else if (selector == 4)
    {
        __execl("TACP.exe", "TACP.exe", 5, 5, 0);
    }

    remove_timer();
    close_music();
    close_sound();
    close_video();
    exit(1);
*/
}


